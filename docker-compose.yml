services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nimshop
      POSTGRES_PASSWORD: nimshop
      POSTGRES_DB: nimshop
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: